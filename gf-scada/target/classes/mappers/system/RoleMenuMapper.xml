<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ginfon.core.web.mapper.RoleMenuMapper">
	<resultMap type="com.ginfon.core.web.entity.RoleMenu" id="RoleMenuResult">
		<result property="roleId"     column="role_id"      />
		<result property="menuId"     column="menu_id"      />
	</resultMap>

	<delete id="deleteRoleMenuByRoleId" parameterType="Long">
		delete from sys_role_menu where role_id=#{roleId} and (flag = 0 or flag = 2)
	</delete>
	
	<select id="selectCountRoleMenuByMenuId" resultType="Integer">
	    select count(1) from sys_role_menu where menu_id=#{menuId} and (flag = 0 or flag = 2)
	</select>
	
	<delete id="deleteRoleMenu" parameterType="Long">
 		delete from sys_role_menu where role_id in
 		<foreach collection="array" item="roleId" open="(" separator="," close=")">
 			#{roleId}
        </foreach>
		and (flag = 0 or flag = 2)
 	</delete>
	
	<insert id="batchRoleMenu">
		insert into sys_role_menu(role_id, menu_id, flag) values
		<foreach item="item" index="index" collection="list" separator=",">
			(#{item.roleId},#{item.menuId},2)
		</foreach>
	</insert>
	
</mapper> 